---
// Hero.astro
// Mood Digital: warm palette + editorial type + animated grid/checker background
---

<section class="hero" id="top">
  <div class="hero-bg" aria-hidden="true"></div>
  <div class="hero-fade" aria-hidden="true"></div>

  <div class="wrap">
    <div class="grid">
      <div class="copy">
        <div class="pill fade-in">
          <span class="dot" aria-hidden="true"></span>
          <span class="pill-text">Canva. Business. Identite.</span>
        </div>

        <h1 class="fade-in delay-1">
          Tu n'as pas besoin de faire plus.
          <span class="h1-break"><br /></span>
          Tu as besoin d'un <span class="accent">cadre</span>.
        </h1>

        <p class="subtitle fade-in delay-2">
          Formation Canva et accompagnements business pour creatrices qui veulent une image forte,
          des visuels coherents, et une structure qui tient. Moins de "jolis visuels". Plus d'identite.
        </p>

        <div class="cta fade-in delay-3">
          <a class="btn btn-primary" href="#kit" aria-label="Recevoir le kit gratuit">
            Recevoir le Kit Mood Express
            <span class="btn-icon" aria-hidden="true">→</span>
          </a>
          <a class="btn btn-secondary" href="#formation" aria-label="Voir la formation Canva">
            Voir la formation
          </a>
        </div>

        <div class="meta fade-in delay-4" role="list">
          <div class="chip" role="listitem">
            <span class="k">Pour</span>
            <span class="v">Entrepreneuses, creatrices</span>
          </div>
          <div class="chip" role="listitem">
            <span class="k">Focus</span>
            <span class="v">Brand + system Canva</span>
          </div>
          <div class="chip" role="listitem">
            <span class="k">Resultat</span>
            <span class="v">Identite stable, contenu rapide</span>
          </div>
        </div>
      </div>

      <div class="visual" aria-hidden="true">
        <div class="board" id="moodBoard">
          <div class="tile t-cocoa"></div>
          <div class="tile t-moss"></div>

          <div class="tile t-lilac"></div>
          <div class="tile t-wood"></div>

          <div class="tile t-lamp">
            <div class="lamp-glow"></div>
            <div class="lamp"></div>
            <div class="base"></div>
          </div>

          <div class="tile t-checker"></div>
          <div class="tile t-metal"></div>

          <div class="star">
            <svg viewBox="0 0 100 100" class="star-svg" role="presentation">
              <polygon points="50,6 58,28 80,20 72,42 94,50 72,58 80,80 58,72 50,94 42,72 20,80 28,58 6,50 28,42 20,20 42,28" />
            </svg>
          </div>

          <div class="stamp">
            <span class="stamp-top">moodigital.co</span>
            <span class="stamp-mid">canva mood</span>
            <span class="stamp-bot">direction creatrice</span>
          </div>
        </div>

        <div class="caption">
          <span class="cap-1">Moins communication.</span>
          <span class="cap-2">Plus creation d'univers.</span>
        </div>
      </div>
    </div>
  </div>
</section>

<script is:inline>
  // Grid and board react on desktop, drift on mobile.
  const root = document.documentElement;
  const hero = document.querySelector(".hero");

  const isFinePointer = window.matchMedia && window.matchMedia("(pointer:fine)").matches;
  const reduceMotion = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  if (hero && isFinePointer && !reduceMotion) {
    let raf = 0;
    const onMove = (e) => {
      cancelAnimationFrame(raf);
      raf = requestAnimationFrame(() => {
        const r = hero.getBoundingClientRect();
        const x = (e.clientX - r.left) / r.width;
        const y = (e.clientY - r.top) / r.height;
        root.style.setProperty("--mx", x.toFixed(3));
        root.style.setProperty("--my", y.toFixed(3));
      });
    };

    hero.addEventListener("pointermove", onMove);
    hero.addEventListener("pointerleave", () => {
      root.style.setProperty("--mx", "0.5");
      root.style.setProperty("--my", "0.35");
    });
  }
</script>

<style>
  :root {
    --mx: 0.5;
    --my: 0.35;

    --bg-0: #070607;
    --bg-1: #0c0a08;

    --text: #f3f3f4;
    --muted: rgba(243, 243, 244, 0.74);

    --cream: #e3dbcf;
    --lilac: #bbadb5;
    --cocoa: #7e441e;
    --moss: #223026;
    --orange: #e6730a;

    --glass: rgba(255,255,255,0.10);
    --ease: cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hero {
    position: relative;
    overflow: clip;
    padding: 7.5rem 1.25rem 6.25rem;
    min-height: 88vh;
    display: grid;
    align-items: center;
    color: var(--text);
    background: transparent;
    font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  }

  /* Background layers (animated grid + checker + color washes). */
  .hero-bg {
    position: absolute;
    inset: -2px;
    z-index: -2;

    background:
      radial-gradient(900px 700px at calc(var(--mx) * 100%) calc(var(--my) * 100%), rgba(230,115,10,0.14), transparent 60%),
      radial-gradient(900px 650px at calc((1 - var(--mx)) * 100%) 20%, rgba(34,48,38,0.22), transparent 62%),
      radial-gradient(700px 520px at 18% 18%, rgba(187,173,181,0.14), transparent 66%),
      linear-gradient(180deg, var(--bg-1), var(--bg-0));

    /* Grid on top of gradients */
    mask-image: linear-gradient(to bottom, transparent 0%, #000 12%, #000 78%, transparent 100%);
    -webkit-mask-image: linear-gradient(to bottom, transparent 0%, #000 12%, #000 78%, transparent 100%);
  }

  .hero-bg::before {
    content: "";
    position: absolute;
    inset: 0;
    background:
      repeating-linear-gradient(0deg, rgba(227,219,207,0.08) 0 1px, transparent 1px 54px),
      repeating-linear-gradient(90deg, rgba(227,219,207,0.08) 0 1px, transparent 1px 54px);

    opacity: 0.55;
    transform: translate3d(0, 0, 0);
    animation: gridDrift 10s var(--ease) infinite alternate;
  }

  .hero-bg::after {
    content: "";
    position: absolute;
    inset: 0;

    /* Checker accent like Mood Digital, softly blended */
    background:
      linear-gradient(45deg, rgba(0,0,0,0.84) 25%, transparent 25%) 0 0 / 44px 44px,
      linear-gradient(-45deg, rgba(0,0,0,0.84) 25%, transparent 25%) 0 22px / 44px 44px,
      linear-gradient(45deg, transparent 75%, rgba(0,0,0,0.84) 75%) 22px -22px / 44px 44px,
      linear-gradient(-45deg, transparent 75%, rgba(0,0,0,0.84) 75%) 22px 0 / 44px 44px;

    opacity: 0.14;
    mix-blend-mode: overlay;
    transform: translate3d(calc((var(--mx) - 0.5) * -18px), calc((var(--my) - 0.5) * -16px), 0);
    filter: blur(0.15px);
  }

  @keyframes gridDrift {
    0% { transform: translate3d(-18px, -12px, 0); opacity: 0.45; }
    100% { transform: translate3d(18px, 10px, 0); opacity: 0.60; }
  }

  /* Bottom fade so the next section never cuts hard. */
  .hero-fade {
    position: absolute;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    background:
      radial-gradient(1200px 520px at 50% 0%, rgba(0,0,0,0.40), transparent 55%),
      linear-gradient(to bottom, transparent 0%, transparent 68%, rgba(7,6,7,0.95) 92%, rgba(7,6,7,1) 100%);
  }

  /* Soft grain, subtle but premium. */
  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: -1;
    opacity: 0.10;
    mix-blend-mode: overlay;
    background-image:
      repeating-radial-gradient(circle at 20% 30%, rgba(255,255,255,0.35) 0 1px, transparent 1px 7px),
      repeating-radial-gradient(circle at 80% 60%, rgba(0,0,0,0.45) 0 1px, transparent 1px 8px);
    filter: blur(0.2px);
  }

  .wrap { width: min(1180px, 100%); margin: 0 auto; }

  .grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 3.2rem;
    align-items: center;
  }

  .pill {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.55rem 1rem;
    border-radius: 999px;
    border: 1px solid var(--glass);
    background: rgba(255,255,255,0.03);
    backdrop-filter: blur(12px);
    box-shadow: 0 16px 50px rgba(0,0,0,0.28);
    margin-bottom: 1.6rem;
    transition: transform 0.35s var(--ease), border-color 0.35s var(--ease);
  }
  .pill:hover { transform: translateY(-2px); border-color: rgba(255,255,255,0.16); }

  .dot {
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: var(--orange);
    box-shadow: 0 0 0 5px rgba(230,115,10,0.12);
    animation: pulse 2.4s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(0.88); opacity: 0.62; }
  }

  .pill-text {
    font-size: 0.84rem;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: rgba(227,219,207,0.92);
  }

  h1 {
    font-family: ui-serif, Georgia, Cambria, "Times New Roman", Times, serif;
    font-weight: 780;
    font-size: clamp(2.65rem, 5.4vw, 4.9rem);
    line-height: 1.02;
    letter-spacing: -0.04em;
    margin: 0 0 1.2rem;
  }

  .accent {
    background: linear-gradient(135deg, var(--cream) 0%, rgba(230,115,10,0.95) 72%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .subtitle {
    margin: 0 0 2.2rem;
    color: var(--muted);
    font-size: clamp(1.05rem, 2.0vw, 1.2rem);
    line-height: 1.65;
    max-width: 58ch;
  }

  .cta {
    display: flex;
    gap: 0.9rem;
    flex-wrap: wrap;
    align-items: center;
    margin-bottom: 1.7rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    padding: 0.95rem 1.8rem;
    border-radius: 999px;
    text-decoration: none;
    font-weight: 650;
    border: 1px solid transparent;
    transition: transform 0.35s var(--ease), box-shadow 0.35s var(--ease), background 0.35s var(--ease), border-color 0.35s var(--ease);
    will-change: transform;
  }

  .btn-primary {
    background: var(--cream);
    color: #0b0a08;
    border-color: rgba(227,219,207,0.75);
    box-shadow: 0 18px 55px rgba(0,0,0,0.35);
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 28px 75px rgba(0,0,0,0.48);
  }

  .btn-secondary {
    background: rgba(255,255,255,0.035);
    color: var(--text);
    border-color: rgba(255,255,255,0.12);
    backdrop-filter: blur(10px);
  }

  .btn-secondary:hover {
    transform: translateY(-3px);
    border-color: rgba(255,255,255,0.22);
    background: rgba(255,255,255,0.06);
  }

  .btn-icon {
    display: inline-block;
    transition: transform 0.28s var(--ease);
  }

  .btn-primary:hover .btn-icon { transform: translateX(4px); }

  .meta {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.75rem;
  }

  .chip {
    padding: 0.9rem 1rem;
    border-radius: 18px;
    border: 1px solid rgba(255,255,255,0.08);
    background: rgba(255,255,255,0.02);
    backdrop-filter: blur(12px);
    transition: transform 0.35s var(--ease), border-color 0.35s var(--ease);
  }

  .chip:hover { transform: translateY(-2px); border-color: rgba(255,255,255,0.16); }

  .k {
    display: block;
    font-size: 0.78rem;
    letter-spacing: 0.10em;
    text-transform: uppercase;
    color: rgba(227,219,207,0.82);
    margin-bottom: 0.25rem;
  }

  .v {
    display: block;
    font-size: 0.98rem;
    color: rgba(243,243,244,0.92);
    font-weight: 620;
  }

  .visual { width: min(520px, 100%); justify-self: end; }

  .board {
    position: relative;
    border-radius: 28px;
    padding: 14px;
    background: rgba(255,255,255,0.02);
    border: 1px solid rgba(255,255,255,0.10);
    box-shadow: 0 30px 90px rgba(0,0,0,0.55);
    backdrop-filter: blur(14px);

    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-auto-rows: 118px;
    gap: 12px;

    transform: perspective(1200px)
      rotateX(calc((0.5 - var(--my)) * 8deg))
      rotateY(calc((var(--mx) - 0.5) * 10deg))
      translate3d(calc((var(--mx) - 0.5) * 10px), calc((var(--my) - 0.5) * 10px), 0);

    transition: transform 0.25s var(--ease);
    will-change: transform;

    /* Edge fade so the card feels integrated. */
    mask-image: radial-gradient(120% 120% at 50% 20%, #000 66%, transparent 100%);
    -webkit-mask-image: radial-gradient(120% 120% at 50% 20%, #000 66%, transparent 100%);
  }

  .tile {
    border-radius: 20px;
    border: 1px solid rgba(255,255,255,0.10);
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.08);
    overflow: hidden;
    position: relative;
    transform: translateZ(0);
  }

  .t-cocoa { background: linear-gradient(135deg, rgba(126,68,30,0.95), rgba(92,45,13,0.95)); }
  .t-moss { background: linear-gradient(135deg, rgba(34,48,38,0.95), rgba(16,23,18,0.95)); }
  .t-lilac { background: linear-gradient(135deg, rgba(187,173,181,0.98), rgba(227,219,207,0.76)); }

  .t-wood {
    background:
      linear-gradient(90deg, rgba(0,0,0,0.18), transparent 45%),
      repeating-linear-gradient(
        90deg,
        rgba(126, 68, 30, 0.92) 0px,
        rgba(126, 68, 30, 0.92) 14px,
        rgba(92, 45, 13, 0.90) 14px,
        rgba(92, 45, 13, 0.90) 26px,
        rgba(140, 86, 50, 0.88) 26px,
        rgba(140, 86, 50, 0.88) 38px
      );
    filter: saturate(1.06);
  }

  .t-lamp {
    background:
      radial-gradient(220px 160px at 60% 35%, rgba(230,115,10,0.20), transparent 70%),
      linear-gradient(180deg, rgba(0,0,0,0.26), rgba(0,0,0,0.62));
  }

  .lamp-glow {
    position: absolute;
    inset: -40px;
    background: radial-gradient(circle at 55% 40%, rgba(230,115,10,0.26), transparent 60%);
    filter: blur(10px);
    opacity: 0.9;
  }

  .lamp {
    position: absolute;
    left: 50%;
    top: 18px;
    width: 70px;
    height: 150px;
    transform: translateX(-50%);
    border-radius: 40px;
    background: linear-gradient(180deg, rgba(255,255,255,0.12), rgba(0,0,0,0.18)), var(--orange);
    box-shadow: 0 22px 65px rgba(230,115,10,0.22);

    /* wavy silhouette */
    clip-path: polygon(
      50% 0%,
      66% 6%,
      78% 16%,
      70% 26%,
      54% 34%,
      66% 44%,
      78% 56%,
      66% 66%,
      52% 74%,
      70% 84%,
      78% 92%,
      50% 100%,
      22% 92%,
      30% 84%,
      48% 74%,
      34% 66%,
      22% 56%,
      34% 44%,
      46% 34%,
      30% 26%,
      22% 16%,
      34% 6%
    );
  }

  .base {
    position: absolute;
    left: 50%;
    bottom: 12px;
    width: 120px;
    height: 18px;
    transform: translateX(-50%);
    border-radius: 999px;
    background: linear-gradient(90deg, rgba(227,219,207,0.24), rgba(255,255,255,0.08), rgba(227,219,207,0.18));
    border: 1px solid rgba(255,255,255,0.10);
  }

  .t-checker {
    background:
      linear-gradient(45deg, rgba(0,0,0,0.92) 25%, transparent 25%) 0 0 / 40px 40px,
      linear-gradient(-45deg, rgba(0,0,0,0.92) 25%, transparent 25%) 0 20px / 40px 40px,
      linear-gradient(45deg, transparent 75%, rgba(0,0,0,0.92) 75%) 20px -20px / 40px 40px,
      linear-gradient(-45deg, transparent 75%, rgba(0,0,0,0.92) 75%) 20px 0 / 40px 40px,
      linear-gradient(135deg, rgba(243,243,244,0.95), rgba(227,219,207,0.92));
    filter: contrast(1.03);
  }

  .t-metal {
    background:
      repeating-linear-gradient(90deg, rgba(255,255,255,0.08) 0 2px, rgba(0,0,0,0.06) 2px 5px),
      linear-gradient(90deg, rgba(207,211,215,0.92), rgba(154,160,167,0.78), rgba(227,230,234,0.90));
  }

  .star {
    position: absolute;
    left: 46%;
    top: 44%;
    width: 64px;
    height: 64px;
    transform: translate(-50%, -50%);
    filter: drop-shadow(0 16px 40px rgba(0,0,0,0.35));
    animation: floatStar 6.5s ease-in-out infinite;
    pointer-events: none;
  }

  .star-svg polygon { fill: var(--orange); opacity: 0.96; }

  @keyframes floatStar {
    0%, 100% { transform: translate(-50%, -50%) rotate(0deg); }
    50% { transform: translate(-50%, calc(-50% - 8px)) rotate(6deg); }
  }

  .stamp {
    position: absolute;
    right: 16px;
    top: 16px;
    padding: 0.75rem 0.9rem;
    border-radius: 18px;
    border: 1px solid rgba(255,255,255,0.12);
    background: rgba(0,0,0,0.22);
    backdrop-filter: blur(10px);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    line-height: 1.2;
    color: rgba(227,219,207,0.88);
    box-shadow: 0 18px 55px rgba(0,0,0,0.40);
  }

  .stamp-top { display: block; font-size: 0.68rem; opacity: 0.9; }
  .stamp-mid { display: block; font-size: 0.78rem; font-weight: 700; color: rgba(243,243,244,0.92); margin: 0.18rem 0; }
  .stamp-bot { display: block; font-size: 0.68rem; opacity: 0.85; }

  .caption {
    margin-top: 0.9rem;
    display: flex;
    gap: 0.5rem;
    justify-content: center;
    color: rgba(227,219,207,0.85);
    font-size: 0.95rem;
  }

  .cap-1 { opacity: 0.82; }
  .cap-2 { opacity: 1; font-weight: 700; }

  /* Reveal animations */
  .fade-in {
    opacity: 0;
    transform: translateY(26px) scale(0.985);
    filter: blur(10px);
    animation: reveal 1.15s var(--ease) forwards;
    will-change: opacity, transform, filter;
  }

  .delay-1 { animation-delay: 0.12s; }
  .delay-2 { animation-delay: 0.26s; }
  .delay-3 { animation-delay: 0.40s; }
  .delay-4 { animation-delay: 0.54s; }

  @keyframes reveal {
    to { opacity: 1; transform: translateY(0) scale(1); filter: blur(0); }
  }

  /* Mobile behavior: drift stays automatic even without hover. */
  @media (max-width: 980px) {
    .grid { grid-template-columns: 1fr; gap: 2.2rem; }
    .visual { justify-self: start; width: min(560px, 100%); }
    .meta { grid-template-columns: 1fr; }
    .hero-bg::before { animation-duration: 8.5s; }
  }

  @media (max-width: 560px) {
    .hero { padding-top: 5.6rem; min-height: auto; }
    .cta { flex-direction: column; align-items: stretch; }
    .btn { justify-content: center; width: 100%; }
    .board { grid-auto-rows: 110px; }
    .star { width: 56px; height: 56px; }
    .h1-break { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    .fade-in { animation: none; opacity: 1; transform: none; filter: none; }
    .star { animation: none; }
    .dot { animation: none; }
    .hero-bg::before { animation: none; }
    .board { transition: none; transform: none; }
  }
</style>
