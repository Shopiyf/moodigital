---
// Hero.astro - Version Mood Digital (Grid & Glow)
---

<section class="hero-wrapper">
  
  <!-- FOND : La Grille Technique + Le Glow -->
  <div class="grid-bg"></div>
  <div class="spotlight"></div>

  <div class="container">
    
    <!-- TOP : Badge Status -->
    <div class="status-pill fade-in">
      <span class="status-dot"></span>
      <span class="status-text">Available for new projects</span>
    </div>

    <!-- CENTRE : Titre Monumental -->
    <div class="hero-content">
      <h1 class="fade-in delay-1">
        We craft <span class="highlight">digital</span><br />
        <span class="outline">experiences.</span>
      </h1>

      <p class="subtitle fade-in delay-2">
        Mood Digital transforme votre vision en interface d'exception.<br class="desktop-br"/>
        Design stratégique. Développement créatif. Impact réel.
      </p>

      <!-- BOUTONS : Style "Glass" très fin -->
      <div class="cta-group fade-in delay-3">
        <a href="#contact" class="btn btn-primary">
          Start a project
        </a>
        <a href="#work" class="btn btn-ghost">
          View work
        </a>
      </div>
    </div>

    <!-- BOTTOM : Infos Techniques (Style Moodboard) -->
    <div class="hero-footer fade-in delay-4">
      <div class="spec-item">
        <span class="label">Location</span>
        <span class="val">Paris, FR</span>
      </div>
      <div class="spec-item">
        <span class="label">Specialty</span>
        <span class="val">Web & Mobile</span>
      </div>
      <div class="spec-item">
        <span class="label">Est.</span>
        <span class="val">2024</span>
      </div>
    </div>

  </div>
</section>

<style>
  /* --- 1. CONFIGURATION --- */
  :root {
    --bg-dark: #050505; /* Noir presque pur */
    --grid-color: rgba(255, 255, 255, 0.03);
    --glow-color: rgba(60, 100, 255, 0.15); /* Bleu électrique très subtil */
    --text-main: #ffffff;
    --text-muted: #888888;
    --border-color: rgba(255, 255, 255, 0.1);
    --ease-smooth: cubic-bezier(0.2, 0.8, 0.2, 1);
  }

  /* --- 2. STRUCTURE & FOND --- */
  .hero-wrapper {
    position: relative;
    background-color: var(--bg-dark);
    color: var(--text-main);
    min-height: 100vh; /* Plein écran */
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    padding-top: 80px; /* Pour le header */
  }

  /* La Grille (L'effet Moodboard) */
  .grid-bg {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(var(--grid-color) 1px, transparent 1px),
      linear-gradient(90deg, var(--grid-color) 1px, transparent 1px);
    background-size: 60px 60px; /* Taille des carreaux */
    z-index: 1;
    mask-image: radial-gradient(circle at center, black 40%, transparent 100%);
  }

  /* Le Spot lumineux central */
  .spotlight {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 800px;
    height: 800px;
    background: radial-gradient(circle, var(--glow-color) 0%, transparent 70%);
    filter: blur(80px);
    z-index: 0;
    animation: pulse 8s ease-in-out infinite alternate;
  }

  .container {
    position: relative;
    z-index: 10;
    width: 100%;
    max-width: 1200px;
    padding: 0 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    height: 100%;
    justify-content: center;
  }

  /* --- 3. TYPOGRAPHIE & CONTENU --- */
  
  /* Badge Status */
  .status-pill {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 12px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-color);
    border-radius: 100px;
    margin-bottom: 2rem;
    backdrop-filter: blur(5px);
  }
  .status-dot {
    width: 6px; height: 6px;
    background-color: #22c55e; /* Vert status */
    border-radius: 50%;
    box-shadow: 0 0 8px #22c55e;
  }
  .status-text {
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    font-weight: 500;
  }

  /* Titre H1 */
  h1 {
    font-size: clamp(3rem, 8vw, 6.5rem);
    line-height: 1.05;
    font-weight: 700;
    letter-spacing: -0.03em;
    margin-bottom: 1.5rem;
  }
  
  .highlight {
    color: #fff;
    /* Petit effet de dégradé sur le texte */
    background: linear-gradient(to bottom right, #fff 30%, #999 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  
  /* Texte contour (Outline) pour le style architectural */
  .outline {
    color: transparent;
    -webkit-text-stroke: 1px rgba(255, 255, 255, 0.4);
    opacity: 0.8;
  }

  .subtitle {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--text-muted);
    max-width: 600px;
    line-height: 1.6;
    margin: 0 auto 3rem;
  }

  /* --- 4. BOUTONS (Style Minimaliste) --- */
  .cta-group {
    display: flex;
    gap: 16px;
    justify-content: center;
  }

  .btn {
    height: 48px;
    padding: 0 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 6px; /* Coins moins ronds pour faire "Tech" */
    font-weight: 500;
    font-size: 15px;
    text-decoration: none;
    transition: all 0.3s var(--ease-smooth);
  }

  .btn-primary {
    background: #fff;
    color: #000;
  }
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(255, 255, 255, 0.1);
  }

  .btn-ghost {
    background: transparent;
    border: 1px solid var(--border-color);
    color: #fff;
  }
  .btn-ghost:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: #fff;
  }

  /* --- 5. FOOTER DU HERO (Grid Specs) --- */
  .hero-footer {
    position: absolute;
    bottom: 40px;
    left: 0;
    width: 100%;
    display: flex;
    justify-content: space-between;
    padding: 0 40px;
    border-top: 1px solid var(--border-color);
    padding-top: 20px;
    max-width: 1200px;
    left: 50%;
    transform: translateX(-50%);
  }

  .spec-item {
    display: flex;
    flex-direction: column;
    text-align: left;
    gap: 4px;
  }
  .spec-item .label {
    font-size: 10px;
    text-transform: uppercase;
    color: #555;
    letter-spacing: 0.1em;
  }
  .spec-item .val {
    font-size: 13px;
    color: #fff;
    font-family: monospace; /* Touche technique */
  }

  /* --- 6. ANIMATIONS --- */
  @keyframes pulse {
    0% { opacity: 0.5; transform: translate(-50%, -50%) scale(1); }
    100% { opacity: 0.8; transform: translate(-50%, -50%) scale(1.1); }
  }

  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: reveal 1s var(--ease-smooth) forwards;
  }
  .delay-1 { animation-delay: 0.1s; }
  .delay-2 { animation-delay: 0.2s; }
  .delay-3 { animation-delay: 0.3s; }
  .delay-4 { animation-delay: 0.8s; }

  @keyframes reveal {
    to { opacity: 1; transform: translateY(0); }
  }

  /* Mobile */
  @media (max-width: 768px) {
    .desktop-br { display: none; }
    .hero-footer { display: none; } /* On cache les specs sur mobile */
    .grid-bg { background-size: 40px 40px; }
  }
</style>